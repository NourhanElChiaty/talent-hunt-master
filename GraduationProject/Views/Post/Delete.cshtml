@model GraduationProject.Models.PostTable
@{
}
<link rel="stylesheet" href="~/Content/layout_style.css" />

<div class="Confirma">
    <h3>Are you sure you want to delete this?</h3>
    <div class="con">
        <h4 style="font-size:x-large">Post</h4>
    </div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Title)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Title)
            </dd>



        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color con">
                <input type="submit" value="Delete" onclick="location.href='@Url.Action("Index","Post" )';" class="btn btn-default" />
            
            </div>
        }
    </div>
